/// Types de gestes utilisés dans l'application
enum GestureType {
  tap,
  doubleTap,
  longPress,
  verticalDrag,
  horizontalDrag,
}

/// Thèmes disponibles
enum BrightnessSelection {
  system,
  light,
  dark,
}
